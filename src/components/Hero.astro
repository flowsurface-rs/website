---
import { Image } from "astro:assets";
import heroImg from "../assets/hero.png";
---

<section
  class="relative py-[clamp(5.5rem,10vw,9rem)]"
  aria-labelledby="hero-title"
>
  <div
    class="w-full max-w-[1040px] mx-auto px-8 flex flex-col items-center text-center gap-7"
  >
    <div
      class="inline-flex items-center gap-2 bg[rgba(67,166,129,0.18)] bg-[rgba(67,166,129,0.18)] text-accent-primary text-[0.62rem] font-semibold tracking-ultra py-[0.42rem] px-[0.65rem] pb-[0.38rem] border border-[rgba(67,166,129,0.32)] rounded-full uppercase"
      aria-label="Project status"
    >
      Open Source • Free
    </div>
    <h1
      id="hero-title"
      class="text-2xl md:text-5xl leading-[1.04] tracking-tighter font-semibold max-w-[29ch]"
    >
      Analyze <strong class="font-semibold">Liquidity</strong> & <strong
        class="font-semibold">Order Flow</strong
      ><br />
      Across crypto derivatives markets.
    </h1>
    <p
      class="text-text-secondary max-w-[640px] text-[0.85rem] leading-relaxed font-medium tracking-wide"
    >
      Real‑time microstructure workstation: historical DOM heatmap, execution
      footprint clustering, adaptive aggregation, depth ladder & pace audio —
      processed locally in a transparent Rust core. No vendor lock‑in. You own
      the edge.
    </p>
    <div class="flex items-center gap-3 flex-wrap justify-center mt-2">
      <a
        href="#download"
        class="btn btn-primary"
        aria-label="Download Flowsurface">Download</a
      >
      <a
        href="https://github.com/flowsurface-rs/flowsurface"
        class="btn btn-secondary"
        target="_blank"
        rel="noopener"
        aria-label="View Flowsurface on GitHub">GitHub</a
      >
    </div>
    <div
      class="flex gap-6 mt-2 opacity-70 text-[11px] tracking-[0.2em] uppercase font-semibold"
    >
      <span>Heatmap</span>
      <span>Footprint</span>
      <span class="hidden md:block">DOM</span>
      <span class="hidden md:block">Tape</span>
      <span class="hidden md:block">Multi-exchange</span>
      <span class="hidden md:block">Layouts</span>
      <a href="#features" class="whitespace-nowrap">More features ↓</a>
    </div>
    <div class="relative w-full mt-5" aria-hidden="true">
      <div class="relative rounded-3xl bg-gradient-frame p-[3px] shadow-lg">
        <div
          class="relative rounded-[21px] bg-[#0f1010] min-h-[430px] flex items-center justify-center overflow-hidden shadow-frame-inset"
        >
          <Image
            src={heroImg}
            alt="Flowsurface application multi-pane orderflow view"
            class="w-full h-full object-cover block"
            loading="eager"
            format="avif"
            widths={[480, 768, 1024, 1440, 1920]}
            sizes="(max-width: 1040px) 100vw, 1040px"
          />
        </div>
      </div>
    </div>
  </div>
</section>
