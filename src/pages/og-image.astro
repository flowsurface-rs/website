---
import "@fontsource-variable/azeret-mono";
import "../styles/global.css";
---

<html lang="en" class="light">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=1200, height=630" />
    <meta name="robots" content="noindex" />
    <title>Flowsurface OG Image</title>
    <style>
      body {
        margin: 0;
        padding: 0;
        width: 1200px;
        height: 630px;
        overflow: hidden;
        display: flex;
        align-items: center;
        justify-content: center;
        background: linear-gradient(180deg, #fffffc, #f8fbf8 65%);
        font-family: "Azeret Mono", monospace;
      }
      body::before {
        content: "";
        position: fixed;
        inset: 0;
        background-image: linear-gradient(
            rgba(0, 0, 0, 0.05) 1px,
            transparent 1px
          ),
          linear-gradient(90deg, rgba(0, 0, 0, 0.05) 1px, transparent 1px);
        background-size:
          60px 60px,
          60px 60px;
        pointer-events: none;
        opacity: 0.32;
        z-index: 0;
      }
      .container {
        position: relative;
        z-index: 1;
        width: 100%;
        height: 100%;
        display: grid;
        grid-template-columns: 1fr 1fr;
        gap: 60px;
        align-items: center;
        padding: 60px 80px;
        box-sizing: border-box;
      }
      .left-content {
        display: flex;
        flex-direction: column;
        align-items: flex-start;
      }
      .badge {
        display: inline-flex;
        align-items: center;
        gap: 8px;
        background: rgba(44, 123, 92, 0.12);
        color: #2c7b5c;
        font-size: 13px;
        font-weight: 600;
        letter-spacing: 0.15em;
        padding: 10px 16px 8px;
        border: 1px solid rgba(44, 123, 92, 0.25);
        border-radius: 50px;
        text-transform: uppercase;
        margin-bottom: 32px;
      }
      h1 {
        font-size: 52px;
        line-height: 1.12;
        letter-spacing: -0.03em;
        font-weight: 600;
        color: #1e2125;
        margin: 0 0 20px 0;
        text-align: left;
      }
      h1 strong {
        font-weight: 600;
        color: #2c7b5c;
      }
      .tagline {
        font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto,
          "Helvetica Neue", Arial, sans-serif;
        font-size: 18px;
        line-height: 1.5;
        font-weight: 500;
        color: #5a6472;
        margin: 0 0 24px 0;
        text-align: left;
        letter-spacing: 0.01em;
      }
      .platforms {
        display: flex;
        gap: 10px;
        margin-top: 8px;
      }
      .platform-badge {
        font-size: 11px;
        font-weight: 600;
        letter-spacing: 0.08em;
        text-transform: uppercase;
        color: #5a6472;
        background: rgba(0, 0, 0, 0.04);
        padding: 6px 12px 5px;
        border-radius: 6px;
        border: 1px solid rgba(0, 0, 0, 0.08);
      }
      .screenshot-wrapper {
        position: relative;
        width: 100%;
        height: 100%;
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
        gap: 16px;
      }
      .screenshot {
        width: 100%;
        height: auto;
        border-radius: 6px;
        border: 1px solid rgba(0, 0, 0, 0.08);
        box-shadow: 0 8px 32px -8px rgba(0, 0, 0, 0.15);
      }
      .footer {
        font-size: 15px;
        font-weight: 600;
        letter-spacing: 0.08em;
        color: #a0a7b3;
        text-align: center;
      }
    </style>
  </head>
  <body>
    <div class="container">
      <div class="left-content">
        <div class="badge">Open Source • Free</div>
        <h1>
          Analyze <strong>Liquidity</strong> &<br /><strong>Order Flow</strong>
        </h1>
        <p class="tagline">
          Real‑time market microstructure workstation for crypto derivatives
        </p>
        <div class="platforms">
          <div class="platform-badge">Windows</div>
          <div class="platform-badge">macOS</div>
          <div class="platform-badge">Linux</div>
        </div>
      </div>
      <div class="screenshot-wrapper">
        <img
          src={"/hero.png"}
          alt="Flowsurface application screenshot"
          class="screenshot"
          width={520}
        />
        <div class="footer">flowsurface.com</div>
      </div>
    </div>
  </body>
</html>
